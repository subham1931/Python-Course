#TRuple data type
#tuple can store any datatype
#tuples are immutable
#tuples are faster then lists

tup = ('one','two','three')
##Methods we can use
#count
print(tup.count("one"))

#index
print(tup.index('two'))

#index
print(tup[0])

#slicing
print(tup[1:3])

#len
print(len(tup))